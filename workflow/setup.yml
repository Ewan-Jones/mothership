name: setup
version: '1.0'
description: setup workflow for testing peri agent
defaults:
  retry: 1
  timeout: 120
env:
  RUST_BACKTRACE: '1'
nodes:
  - id: shell_test
    type: shell
    run: echo "hello"
  - id: step-4
    type: shell
    depends:
      - shell_test
    run: pwd
